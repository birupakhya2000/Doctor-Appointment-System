$(document).ready(function () {    loadData();});       function loadData() {        $.ajax({            url: '/AdminDemo/GetTimeSlotDoctor',            type: 'GET',            dataType: 'json',            success: function (data) {                renderTable(data);            },            error: function () {                alert('Error occurred while retrieving data.');            }        });    }    function renderTable(data) {        var tableBody = $('#tblDoctorTimeSlot tbody');        tableBody.empty();               $.each(data, function (index, item) {            var fromDateTime = new Date(item.fromTime);            var toDateTime = new Date(item.toTime);            var fromDate = fromDateTime.toLocaleDateString('en-US', { day: '2-digit', month: '2-digit', year: 'numeric' });            var fromTime = fromDateTime.toLocaleTimeString('en-US', { hour: 'numeric', minute: 'numeric', hour12: true });            fromTime = fromTime.replace(':', ':');            var toDate = toDateTime.toLocaleDateString('en-US', { day: '2-digit', month: '2-digit', year: 'numeric' });            var toTime = toDateTime.toLocaleTimeString('en-US', { hour: 'numeric', minute: 'numeric', hour12: true });            toTime = toTime.replace(':', ':');            var formattedFromTime = fromDate + ' | ' + fromTime;            var formattedToTime = toDate + ' | ' + toTime;            var row = '<tr>' +                '<td class="text-center">' + (index + 1) + '</td>' +                '<td class="text-center">' + item.doctorName + '</td>' +                '<td class="text-center">' + formattedFromTime + '</td>' +                '<td class="text-center">' + formattedToTime + '</td>' +                '<td class="text-center">' + fromTime + ' - ' + toTime + '</td>' +                '</tr>';            tableBody.append(row);        });    }//End(This is for showing TimeSlotDoctor table data in tabilar from by using foreign key Doctorname is get by foreign key)